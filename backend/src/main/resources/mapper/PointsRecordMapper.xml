<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.book.mapper.PointsRecordMapper">

    <resultMap id="BaseResultMap" type="com.example.book.entity.PointsRecord">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="points_change" property="pointsChange"/>
        <result column="points_after" property="pointsAfter"/>
        <result column="source_type" property="sourceType"/>
        <result column="source_id" property="sourceId"/>
        <result column="description" property="description"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <select id="findByUserAndSource" resultMap="BaseResultMap">
        SELECT id, user_id, points_change, points_after, source_type, source_id, description, created_at
        FROM points_record
        WHERE user_id = #{userId} AND source_type = #{sourceType} AND source_id = #{sourceId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO points_record (user_id, points_change, points_after, source_type, source_id, description)
        VALUES (#{userId}, #{pointsChange}, #{pointsAfter}, #{sourceType}, #{sourceId}, #{description})
    </insert>

    <select id="findByUserId" resultMap="BaseResultMap">
        SELECT id, user_id, points_change, points_after, source_type, source_id, description, created_at
        FROM points_record
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM points_record WHERE user_id = #{userId}
    </select>

</mapper>
